{
  "name": "flankenscore",
  "version": "1.1.0",
  "private": true,
  "description": "Flankiball-Turniere verwalten",
  "scripts": {
    "build": "python build.py",
    "android:sync": "python build.py && npx cap sync android",
    "android:open": "npx cap open android",
    "android:apk": "python build.py && npx cap sync android && cd android && .\\gradlew.bat assembleDebug",
    "android:release": "python build.py && npx cap sync android && cd android && .\\gradlew.bat assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/core": "^8.5.2",
    "@capacitor/filesystem": "^8.1.3",
    "@capacitor/preferences": "^8.0.1",
    "@capacitor/share": "^8.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2"
  }
}
